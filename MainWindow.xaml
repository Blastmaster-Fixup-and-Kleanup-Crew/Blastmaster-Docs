<Window
    x:Class="WordProcessorApp::MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Menu Bar -->
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New Document" Click="New_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="N" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="New from Template (.dctx)..." Click="NewFromTemplate_Click"/>
                <MenuFlyoutItem Text="Open..." Click="Open_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="O" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Close" Click="Close_Click"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Save" Click="Save_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Save As Document (.dccx)..." Click="SaveAs_Click"/>
                <MenuFlyoutItem Text="Save As Template (.dctx)..." Click="SaveAsTemplate_Click"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Page Setup..." Click="PageSetup_Click"/>
                <MenuFlyoutItem Text="Print Preview..." Click="PrintPreview_Click"/>
                <MenuFlyoutItem Text="Print..." Click="Print_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="P" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Properties..." Click="Properties_Click"/>
                <MenuFlyoutItem Text="Send..." Click="Send_Click"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutSubItem x:Name="RecentFilesMenu" Text="Recent Files">
                    <MenuFlyoutItem Text="No Recent Files" IsEnabled="False"/>
                </MenuFlyoutSubItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Exit" Click="Exit_Click"/>
            </MenuBarItem>
        </MenuBar>

        <!-- Formatting Toolbar -->
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right">
            <AppBarToggleButton Icon="Bold" Label="Bold" Click="BoldButton_Click"/>
            <AppBarToggleButton Icon="Italic" Label="Italic" Click="ItalicButton_Click"/>
            <AppBarToggleButton Icon="Underline" Label="Underline" Click="UnderlineButton_Click"/>
            <AppBarSeparator/>
            <AppBarButton Icon="FontIncrease" Label="Increase Size" Click="FontSizeIncrease_Click"/>
            <AppBarButton Icon="FontDecrease" Label="Decrease Size" Click="FontSizeDecrease_Click"/>
        </CommandBar>

        <!-- Main Editor -->
        <RichEditBox x:Name="Editor" 
                     Grid.Row="2" 
                     Margin="10" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap"/>
    </Grid>
</Window>
